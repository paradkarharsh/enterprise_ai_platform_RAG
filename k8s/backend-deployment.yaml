apiVersion: apps/v1
kind: Deployment
metadata:
  name: manthanai-backend
  namespace: manthanai
  labels:
    app: manthanai-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: manthanai-backend
  template:
    metadata:
      labels:
        app: manthanai-backend
    spec:
      containers:
        - name: backend
          image: manthanai/backend:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: manthanai-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "2000m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: manthanai-backend-service
  namespace: manthanai
spec:
  selector:
    app: manthanai-backend
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
